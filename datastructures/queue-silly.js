
//simple queue hehe
JAL = JAL || {};
JAL.stack = JAL.queue || (function(){
  //private members
  var queue = [];

  //public functions

  return {
    push: queue.shift,
    pop: qurue.pop
  };
}());
